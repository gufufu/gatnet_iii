import React from "react"

export default ({pageContext}) => (
    <div>
        <h1>
            {pageContext.title}

            this is from post page
            this is the second line
            third in line
        </h1>
    </div>
);